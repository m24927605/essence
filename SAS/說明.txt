本案跟ESS案(已在518做過初步說明),
只是選顏色的地方是select,
例如該案選深紅色,S尺寸,呈現下拉選單的數量只有,
但選了M尺寸,下拉選單沒作用,並且下面的淺褐色btn出現貨到通知,
最後成品是下拉選擇數量的select可不保留,

應該不必什麼特殊環境, 只是前端工作,狀況,尺寸,數量是模擬的,
到時後端程式心員(aspx)可套程式即可.

如同之前以select選數量即是完工的狀態,
如今只是select換成加減的方式選數量。
